module.exports = {
  getAll: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getArticles: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getCar: (res,response, name)=>{
    const form = {
        name,
        response,
    };
    res.json(form);
  },
  getMoto: (res,response, name)=>{
    const form = {
        name,
        response,
    };
    res.json(form);
  },
  getData: (res,response, name)=>{
    const form = {
        name,
        response,
    };
    res.json(form);
  },
  postProduct: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  addProduct: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  deleteProduct: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  putProduct: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getTransaction: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  postTransaction: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getsubTrans: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  postsubTrans: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getsubTransByID: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getsubTransByDate: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  updateCustomer: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  updateDriver: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getDriverByID: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getDriverItemPhoto: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  
  getDriverStatus: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  updatesubTransByID: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getsubTransByIDCustomer: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  updatesubTransAccept: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  selecedbyDate: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getCustomerByID: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getDriverCar: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getDriverEconomy:(res,response,status)=>{
    const form={
      status,
      response
    }
    res.json(form);
  },
  // getDriverEconomyPlus:(res,response,status)=>{
  //   const form={
  //     status,
  //     response
  //   }
  //   res.json(form);
  // },
  // getDriverBisnis:(res,response,status)=>{
  //   const form={
  //     status,
  //     response
  //   }
  //   res.json(form);
  // },
  postPhoto:(res,response,status)=>{
    const form={
      status,
      response
    }
    res.json(form);
  }, 
  getDriverByProductClass:(res,response,status)=>{
    const form={
      status,
      response
    }
    res.json(form);
  }
  
};






